streamlit
tensorflow==2.18.1
numpy
pandas
matplotlib
pillow
opencv-python-headless
torch --index-url https://download.pytorch.org/whl/cpu
transformers
# Live Camera Mode (📹 Live tab). The app degrades to a snapshot
# fallback if these are absent, so they are not hard requirements.
streamlit-webrtc
av
